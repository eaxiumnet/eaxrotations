---
phase: 03-per-class-rotation-deep-dives
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - EAXWarriorFury/main.lua
autonomous: true
requirements: [WARR-02]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Fury rotation switches to explicit fast-1H execute behavior below 20% target HP"
    - "Fury execute timing is gated by shared swing timer safety checks"
  artifacts:
    - path: "EAXWarriorFury/main.lua"
      provides: "Execute-phase branch with dual-wield speed detection and swing-safe gating"
      contains: "require(\"common/eax_shared/swing_timer\")"
  key_links:
    - from: "EAXWarriorFury/main.lua"
      to: "common/eax_shared/swing_timer.lua"
      via: "execute-phase timing guard"
      pattern: "swing_timer\\.(is_swing_safe|can_cast_before_swing)"
---

<objective>
Close the WARR-02 verification gap by implementing explicit dual-wield fast one-hander execute behavior in Fury below 20% HP, wired to shared swing-timer safety checks.

Purpose: Remove the remaining Warrior execute correctness blocker from phase verification.
Output: Updated `EAXWarriorFury/main.lua` with weapon-speed-aware execute lane and swing-safe timing.
</objective>

<execution_context>
@C:/Users/Support/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Support/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-per-class-rotation-deep-dives/03-VERIFICATION.md
@.planning/phases/03-per-class-rotation-deep-dives/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fury execute prerequisites (swing timer + weapon speed checks)</name>
  <read_first>
    - EAXWarriorFury/main.lua
    - EAXWarriorArms/main.lua
    - common/eax_shared/swing_timer.lua
    - .planning/phases/03-per-class-rotation-deep-dives/03-VERIFICATION.md
  </read_first>
  <files>EAXWarriorFury/main.lua</files>
  <action>
    Update `EAXWarriorFury/main.lua` to require `common/eax_shared/swing_timer` and add a local helper that classifies the current dual-wield setup as "fast-1H" only when both mainhand and offhand weapon speeds are known and <= 2.0 seconds.
    Implement speed reads using existing Sylvanas unit/item APIs already used in this repo (do not invent new APIs); if speed cannot be resolved, return false and keep current non-fast fallback behavior.
    Keep thresholds concrete: execute phase starts at target HP <= 0.20 and fast-1H predicate requires both hands meeting <= 2.0 seconds.
  </action>
  <acceptance_criteria>
    - `EAXWarriorFury/main.lua` contains `require("common/eax_shared/swing_timer")`
    - `EAXWarriorFury/main.lua` contains a helper using both mainhand and offhand speed values
    - `EAXWarriorFury/main.lua` contains a concrete `<= 2.0` fast-weapon threshold check
  </acceptance_criteria>
  <verify>
    <automated>rtk rg -n "common/eax_shared/swing_timer|offhand|mainhand|<=\s*2\.0" EAXWarriorFury/main.lua</automated>
  </verify>
  <done>
    Fury file has deterministic fast-1H detection data needed for execute behavior decisions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce swing-safe fast-1H execute lane below 20%</name>
  <read_first>
    - EAXWarriorFury/main.lua
    - common/eax_shared/swing_timer.lua
    - .planning/phases/03-per-class-rotation-deep-dives/03-VERIFICATION.md
  </read_first>
  <files>EAXWarriorFury/main.lua</files>
  <action>
    In the Fury execute branch (`target_hp <= 0.20`), gate cast/queue decisions through `swing_timer.is_swing_safe(...)` or `swing_timer.can_cast_before_swing(...)` so casts do not clip white swings.
    When fast-1H predicate is true, prioritize the fast execute lane (Execute plus Heroic Strike/Cleave queue behavior) and make this lane explicit in code comments/branch names; when predicate is false, retain existing non-fast behavior.
    Remove ambiguity by deleting any placeholder TODO/fallback wording that implies execute logic is incomplete.
  </action>
  <acceptance_criteria>
    - `EAXWarriorFury/main.lua` has an execute branch keyed on `<= 0.20` HP
    - Execute branch references `swing_timer.is_swing_safe` or `swing_timer.can_cast_before_swing`
    - Execute branch contains a conditional fast-1H decision path and a non-fast fallback path
    - No execute-path TODO comments remain
  </acceptance_criteria>
  <verify>
    <automated>rtk rg -n "0\.20|swing_timer\.(is_swing_safe|can_cast_before_swing)|TODO" EAXWarriorFury/main.lua && rtk rg -n "fast.*1H|one.?hand|offhand" EAXWarriorFury/main.lua</automated>
  </verify>
  <done>
    Fury execute behavior is weapon-speed-aware, swing-safe, and no longer flagged as missing by verification truth #2.
  </done>
</task>

</tasks>

<verification>
Run targeted static verification for Fury gap closure:
- `rtk rg -n "swing_timer|offhand|mainhand|0\.20" EAXWarriorFury/main.lua`
- `rtk rg -n "WARR-02|Fury execute" .planning/phases/03-per-class-rotation-deep-dives/03-VERIFICATION.md`
</verification>

<success_criteria>
`EAXWarriorFury/main.lua` explicitly implements dual fast-1H execute behavior below 20% with swing-timer wiring, satisfying WARR-02 gap requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/03-per-class-rotation-deep-dives/03-10-SUMMARY.md`
</output>

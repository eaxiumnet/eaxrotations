# Community Profiles

Community profile examples are stored as Lua modules so the release package
stays limited to `.lua` and `.md` files.

Each profile file returns a plain table with:

- `name`
- `author`
- `class`
- `spec`
- `patch`
- `description`
- `settings`
- `metadata`
